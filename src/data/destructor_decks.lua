local Const = require("const")

local DestructorDecks = {
  [Const.DESTRUCTOR_DECKS.EMPTY] = {
    cards = {},
  },
  -- add more decks here later with special destructor cards
}

return DestructorDecks
